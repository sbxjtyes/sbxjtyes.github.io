services:
  - type: web
    name: gmail-tempmail
    runtime: python
    rootDir: gmail-plus
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python run.py
    envVars:
      - key: GMAIL_ADDRESS
        sync: false
      - key: GMAIL_APP_PASSWORD
        sync: false
